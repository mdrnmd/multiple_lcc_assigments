#include<stdio.h>

int main() {
  int v[30], conta=0, i, j;

  scanf("%d" ,&v[0]);
  for (i=1; i!=-999;i++ ) {
    scanf("%d" ,&v[i]); }
  for (j=0 ; j<i ; j++) {
    if (v[j] == v[i])
      conta++;
  }
  printf("%d" ,conta);
  return 0;
}
