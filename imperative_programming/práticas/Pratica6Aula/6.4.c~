#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main () {
  int  i, x;
  for (i=0; i<1000; i++) {
    x= 155 + srand((unsigned) time(NULL) ) %56; 
      printf("%.2f\n",(float)x/100 ); 
    }
  
  return 0;
}
